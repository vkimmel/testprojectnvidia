// HelloWorld.App.csproj
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <Deterministic>true</Deterministic>
    <AssemblyTitle>HelloWorld.App</AssemblyTitle>
    <!-- TreatWarningsAsErrors auskommentiert bzw. false, da Einsteiger-Projekt -->
  </PropertyGroup>

  <!-- Import des InstallTargets.targets.
       Wenn die Datei nicht vorhanden ist, kommentiert man die Zeile aus oder erstellt sie gesondert. -->
  <!-- <Import Project="InstallTargets.targets" />  -->

</Project>